function init(self)
	self.isEgg = true
	self.food = 0
	msg.post(".", "acquire_input_focus")
end

function on_message(self, message_id, message, sender)
	print("abc")
	if message_id == hash("food") then
		if not self.isEgg then
			self.food = self.food + message[1]
		end
	end
end